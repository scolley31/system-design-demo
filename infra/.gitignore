# Terraform state 含敏感值（DB 密碼等），絕不可進版控
.terraform/
*.tfstate
*.tfstate.*
tfplan
*.tfplan
*.tfvars
!*.tfvars.example
crash.log
# Lambda 打包產物（apply 時由 null_resource 產生）
modules/cleanup/build/
modules/cleanup/build.zip
# .terraform.lock.hcl 保留進版控（鎖定 provider 版本）
